# Copie para .env e ajuste se necessário.

# Ambiente Flask
FLASK_ENV=development
FLASK_DEBUG=1

# Caminho do banco SQLite (opcional, default: data/analise.db)
# DATABASE_URL=sqlite:///data/analise.db

# HuggingFace cache (opcional, default: data/models)
# HF_HOME=data/models
